     latest notes
      - next step NEEDS to be rewriting the magic numbers for map and region height, and the boundaries. They're ridiculous.
        need to be calculated based off of one value if psosible i.e. the value in level map, not the map height, as that's the index for iterating, not the actual height.
        and also need to separate out region boundaries itno a more readadble method, it's impossible to interupt what values should be atm.

        region boundaries need to be changed. Walls at the bottom of the screen are being cut off.

        LATEST LATEST notes...lol
        - torch now works. I'd prefer it if hallways were discovered with a gradient too.