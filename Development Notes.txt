-- latest note
Still trying to read the code to understand where it was left off. But the idea of a region class might actually be possible?
Since atm I use map level, which stores all map spaces, regardless of region, but I also use regions elsewhere, it suggests i could easily
also use region objectts. The difficult decision is whether I gain anything from this.


Latest idea
- I think a torch function for coridors might still be cool? But rooms will light up as though by a switch
- If each mapspace object has a room object associated with it, then its a simple nested if every player movement
    - does this space belong to a room, yes or no
        - is this room.lightson, if not, call a function to make all room squares visible

- genius idea for the torch 2d array issue...if it's a 2d array of mapspace objects, then updating that object anywhere would update it everywhere
    - so I still only need to update the mapspace one time when it changes e.g. becomes visible



- separate classes for map level model and controller?